name: Stimulate / Testnet

"on":
  schedule:
    - cron: "0 * * * *"

  workflow_dispatch:
    inputs:
      count:
        description: Number of Abstract Accounts to create
        required: false
        default: "5"
        type: string

permissions:
  contents: read

concurrency:
  group: stimulate-testnet
  cancel-in-progress: false

jobs:
  create-abstract-accounts:
    if: github.repository == 'axone-protocol/contracts'
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          ref: main
          persist-credentials: false

      - name: Cache cargo registry
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            ~/.rustup
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Setup Nix
        uses: ./.github/actions/setup-nix

      - name: Validate count
        shell: bash
        env:
          ACCOUNT_COUNT: ${{ github.event.inputs.count || '5' }}
        run: |
          set -euo pipefail

          count="${ACCOUNT_COUNT}"
          if ! [[ "${count}" =~ ^[0-9]+$ ]]; then
            echo "count must be an integer" >&2
            exit 1
          fi
          if (( count < 1 || count > 50 )); then
            echo "count must be between 1 and 50" >&2
            exit 1
          fi

      - name: Create Abstract Accounts on Axone testnet
        env:
          ACCOUNT_COUNT: ${{ github.event.inputs.count || '5' }}
          RUST_LOG: info
          TEST_MNEMONIC: ${{ secrets.AXONE_GITHUB_TESTNET_WALLET }}
        run: nix develop -c cargo make create-abstract-accounts testnet -- --count "${ACCOUNT_COUNT}"
